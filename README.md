
# Socket.IO Fiddle Output

```
$ npm install           # install the dependencies
$ npm start             # run the server

use browser and connect to "http://localhost:3000"

This works:-
npm install socket.io@4.4.1 socket.io-client@4.4.1

This displays parse error
npm install socket.io socket.io-client

```